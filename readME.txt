methods to be followed:
1)install necessary packages for python(streamlit, prophet, yfinance, plotly, smtplib).
1) for every python file open new terminal and run the application using streamlit run python_file_name.py.
3)run index.html to view the web page.